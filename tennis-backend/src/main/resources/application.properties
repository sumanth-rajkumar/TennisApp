# Server configuration
server.port=8080

# Logging configuration
logging.level.org.springframework=INFO

# Path to CSV file (adjust the path according to your setup)
# If the CSV file is in the src/main/resources directory
csv.file.path=classpath:players.csv

# CORS configuration (optional if not set in the code)
# cors.allowed-origins=http://localhost:3000
